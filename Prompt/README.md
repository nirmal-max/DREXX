# DREX Master Prompt

Use `DREX_MASTER_PROMPT.md` as the primary Codex instruction.

Provide Codex with:
1. the DREX GitHub repository;
2. the complete `Architecture.zip`;
3. this master prompt.

The prompt is intentionally implementation-focused while leaving technical architecture choices to Codex.

Expected final release artifact:
`DREX-Setup.exe` built with Inno Setup.

Important: the prompt requires truthful testing and explicitly forbids fabricating a successful build, operation, verification, certificate, or hardware result.
