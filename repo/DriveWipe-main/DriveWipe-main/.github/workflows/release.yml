name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  # Run CI checks before building release artifacts
  ci-gate:
    name: CI Gate (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      # Run every platform to completion. A release gate should report which
      # platforms are broken, not just the first one to fail — cancelling the
      # rest hides whether a failure is platform-specific or universal.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@1.94
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Check
        run: cargo check --workspace
      - name: Test
        run: cargo test --workspace
      - name: Clippy
        run: cargo clippy --workspace -- -D warnings
      - name: Format
        run: cargo fmt --all -- --check

  # The release gate must also refuse to publish artifacts with known
  # vulnerable dependencies; CI checks this on every push, but a tag can be
  # cut from a commit whose advisory status has since changed.
  audit-gate:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit
      - name: Audit dependencies
        run: cargo audit

  build:
    name: Build (${{ matrix.target }})
    needs: [ci-gate, audit-gate]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            suffix: ""
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            suffix: ""
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            suffix: ""
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            suffix: ""
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            suffix: ".exe"
            archive: zip
          - target: aarch64-pc-windows-msvc
            os: windows-latest
            suffix: ".exe"
            archive: zip

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@1.94
        with:
          targets: ${{ matrix.target }}

      - name: Install system dependencies (Linux GUI)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxkbcommon-dev libfontconfig1-dev libasound2-dev

      - name: Install cross-compilation tools (aarch64-linux)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - uses: Swatinem/rust-cache@v2
        with:
          key: release-${{ matrix.target }}

      # One binary carries all three interfaces; it picks between them from how
      # it is invoked.
      - name: Build DriveWipe
        run: cargo build --release --target ${{ matrix.target }} --package drivewipe-cli
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Package artifacts
        shell: bash
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          
          # Human-readable platform names
          case "${{ matrix.target }}" in
            x86_64-unknown-linux-gnu)  PLATFORM="Linux-x64" ;;
            aarch64-unknown-linux-gnu) PLATFORM="Linux-ARM64" ;;
            x86_64-apple-darwin)       PLATFORM="macOS-Intel" ;;
            aarch64-apple-darwin)      PLATFORM="macOS-Apple-Silicon" ;;
            x86_64-pc-windows-msvc)    PLATFORM="Windows-x64" ;;
            aarch64-pc-windows-msvc)   PLATFORM="Windows-ARM64" ;;
            *)                         PLATFORM="${{ matrix.target }}" ;;
          esac

          ARCHIVE_NAME="DriveWipe-${TAG}-${PLATFORM}"
          mkdir -p dist

          cp target/${{ matrix.target }}/release/drivewipe${{ matrix.suffix }} dist/
          cp LICENSE.md dist/
          cp README.md dist/
          cp install.sh dist/ 2>/dev/null || true

          cd dist
          if [ "${{ matrix.archive }}" = "zip" ]; then
            7z a "../${ARCHIVE_NAME}.zip" *
          else
            tar czf "../${ARCHIVE_NAME}.tar.gz" *
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: drivewipe-${{ matrix.target }}
          path: DriveWipe-*.*

  # ── Live ISO Build ──────────────────────────────────────────────────────
  build-live:
    name: Build Live ISO
    needs: [ci-gate, audit-gate]
    # Built on the self-hosted box: the image build needs Docker, loop devices,
    # xorriso and QEMU for the boot check, and is far quicker there than on a
    # hosted runner.
    #
    # The repository guard matters because this is a public repo — a job on a
    # self-hosted runner must never execute code from a fork. This workflow only
    # triggers on tag pushes, which forks cannot perform, and the guard makes
    # that explicit rather than implicit.
    runs-on: [self-hosted, linux, x64, iso-builder]
    if: github.repository == 'KodyDennon/DriveWipe'
    outputs:
      # Whether an ISO was actually produced, so the release notes only
      # advertise it when it exists.
      has_iso: ${{ steps.package-live.outputs.has_iso }}
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@1.94
        with:
          targets: x86_64-unknown-linux-musl

      - uses: Swatinem/rust-cache@v2
        with:
          key: release-live

      - name: Check build prerequisites
        run: |
          missing=""
          for t in docker xorriso mksquashfs isohybrid qemu-system-x86_64 musl-gcc cpio; do
            command -v "$t" >/dev/null || missing="$missing $t"
          done
          if [ -n "$missing" ]; then
            echo "::error::self-hosted runner is missing:$missing"
            exit 1
          fi
          echo "all prerequisites present"

      # The live image is text-mode, and iced does not link against musl, so
      # the GUI is excluded from this build.
      - name: Build DriveWipe for live (static musl, no GUI)
        run: |
          cargo build --release \
            --target x86_64-unknown-linux-musl \
            --package drivewipe-cli \
            --no-default-features --features pdf-report

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep '^version' crates/drivewipe-live/Cargo.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          TAG="${GITHUB_REF#refs/tags/}"
          echo "live_version=${VERSION}" >> $GITHUB_OUTPUT
          echo "tag=${TAG}" >> $GITHUB_OUTPUT

      - name: Build Live ISO
        run: |
          chmod +x scripts/build-live.sh
          DRIVEWIPE_LIVE_VERSION=${{ steps.version.outputs.live_version }} \
            ./scripts/build-live.sh
        env:
          SKIP_KERNEL_MODULE: "1"

      - name: Package Live artifacts
        id: package-live
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          mkdir -p live-dist

          # build-live.sh writes the ISO here and boot-tests it before we get
          # this far, so a missing file means the build genuinely failed.
          cp drivewipe-live.iso "live-dist/drivewipe-live-${TAG}.iso" 2>/dev/null || true

          if [ -f "live-dist/drivewipe-live-${TAG}.iso" ]; then
            echo "has_iso=true" >> $GITHUB_OUTPUT
            echo "::notice::Live ISO built: $(du -h live-dist/drivewipe-live-${TAG}.iso | cut -f1)"
          else
            echo "has_iso=false" >> $GITHUB_OUTPUT
            echo "::warning::No Live ISO was produced; the release will omit it from the notes."
          fi

          # Package PXE boot files (check multiple possible output paths)
          mkdir -p live-dist/pxe
          cp output/vmlinuz live-dist/pxe/ 2>/dev/null || \
          cp target/pxe-server/tftpboot/vmlinuz-lts live-dist/pxe/vmlinuz 2>/dev/null || true
          cp output/initramfs.img live-dist/pxe/ 2>/dev/null || \
          cp target/pxe-server/tftpboot/initramfs-lts live-dist/pxe/initramfs.img 2>/dev/null || true
          cp live/pxe/ipxe/*.ipxe live-dist/pxe/
          cp live/pxe/dnsmasq.conf live-dist/pxe/
          cp live/pxe/README.md live-dist/pxe/

          cd live-dist
          tar czf "../drivewipe-live-${TAG}-pxe.tar.gz" pxe/

      - name: Upload Live artifacts
        uses: actions/upload-artifact@v7
        with:
          name: drivewipe-live
          path: |
            live-dist/drivewipe-live-*.iso
            drivewipe-live-*-pxe.tar.gz

  release:
    name: Create Release
    needs: [build, build-live]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          # Every published artifact must be covered. A narrower glob silently
          # left the desktop archives (DriveWipe-*) unchecksummed while the
          # release notes still told users to verify against this file.
          find . -maxdepth 1 -type f ! -name 'SHA256SUMS.txt' -printf '%P\n' \
            | sort \
            | xargs -r sha256sum > SHA256SUMS.txt
          echo "Checksummed $(wc -l < SHA256SUMS.txt) artifact(s):"
          cat SHA256SUMS.txt

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: DriveWipe ${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: |
            artifacts/*
          body: |
            # 🚀 DriveWipe ${{ steps.version.outputs.version }}

            DriveWipe is a professional-grade storage sanitization and drive management suite. This release provides a full version of the toolkit, including Desktop apps, TUI, and the Live ISO environment.

            ---

            ## 🖥️ Desktop Applications (Standalone)

            No installation required. Download the archive for your system and run the included binaries.

            | Platform | Architecture | Download |
            |:---|:---|:---|
            | **Linux** | x86_64 (Standard 64-bit) | `DriveWipe-${{ steps.version.outputs.version }}-Linux-x64.tar.gz` |
            | **Linux** | ARM64 | `DriveWipe-${{ steps.version.outputs.version }}-Linux-ARM64.tar.gz` |
            | **macOS** | Intel | `DriveWipe-${{ steps.version.outputs.version }}-macOS-Intel.tar.gz` |
            | **macOS** | Apple Silicon | `DriveWipe-${{ steps.version.outputs.version }}-macOS-Apple-Silicon.tar.gz` |
            | **Windows** | x64 (Standard 64-bit) | `DriveWipe-${{ steps.version.outputs.version }}-Windows-x64.zip` |
            | **Windows** | ARM64 | `DriveWipe-${{ steps.version.outputs.version }}-Windows-ARM64.zip` |

            ### 💡 Usage Tip
            DriveWipe requires **Administrative / Root** privileges to access raw disks.
            - **Windows**: Right-click -> "Run as Administrator".
            - **Linux/macOS**: Prefix commands with `sudo`.

            ---

            ## 🔒 Live Environment (For Secure Labs)

            Boot directly into a specialized OS to perform sanitization without host interference.

            | Type | File | Description |
            |:---|:---|:---|
            ${{ needs.build-live.outputs.has_iso == 'true' && format('| **ISO Image** | `drivewipe-live-{0}.iso` | Write to USB with Rufus or `dd`. |', steps.version.outputs.version) || '' }}
            | **PXE Network** | `drivewipe-live-${{ steps.version.outputs.version }}-pxe.tar.gz` | Dissected artifacts for network booting. |

            ---

            ### 🔐 Integrity Verification
            Check the authenticity of your downloads using `SHA256SUMS.txt`:
            ```bash
            sha256sum -c SHA256SUMS.txt
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
