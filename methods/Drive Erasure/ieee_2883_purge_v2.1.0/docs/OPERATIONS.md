# Operations

Default workflow is plan-first. Production UI must require explicit authorization for destructive execution, identity confirmation, offline checks where applicable, and signed evidence. Hardware adapters must be qualified before enabling execute mode.
